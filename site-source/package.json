{
  "name": "merzenich-aktuell",
  "version": "9.0.0",
  "private": true,
  "type": "module",
  "description": "Merzenich Aktuell – Lokalzeitung online. Statischer Generator ohne Abhängigkeiten.",
  "scripts": {
    "build": "node build.mjs && node scripts/release-cleanup.mjs",
    "build:local": "IMAGE_CDN=off node build.mjs && node scripts/release-cleanup.mjs",
    "preview": "IMAGE_CDN=off node build.mjs && node scripts/release-cleanup.mjs && python3 -m http.server 8080 --directory dist",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=20" }
}
